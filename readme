翻译语言：
######################################################################################################################
以英文为模板，翻译。 命令如下：
ltr -i app_en.json -o true -l *

以中文为模板，翻译。 命令如下：

ltr -i app_zh-CN.json -o true -l *

以英文为模板，翻译除中文简体和中文繁体的文件。 命令如下：

ltr -i app_en.json -o true -l * -e zh-CN,zh-TW


翻译中文简体到中文繁体。命令如下：

ltr -i app_zh-CN.json -o true -l zh-TW


----------------------------------------------------------------------------------------------------------------------